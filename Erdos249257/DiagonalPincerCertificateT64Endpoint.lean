import Erdos249257.DiagonalPincerCertificatesT64

/-!
# The t=64 diagonal-pincer certificate endpoint

Compatibility entrypoint for the canonical t=64 certificate module.  The
canonical module includes the standalone exponent-six kill together with the
complete imported-scale summary through t=64.
-/
